fragment itemTypeFragment on ItemType {
    id
    name
    defaultCategory {
        id
        node {
            ...categoryFragment
        }
    }
    defaultIcon {
        id
        node {
            ...iconFragment
        }
    }
}