<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  package="com.apollographql.apollo3.kotlinsample">

  <application
    android:name=".KotlinSampleApp"
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    tools:ignore="GoogleAppIndexingWarning">
    <activity android:name=".repositories.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity
      android:name=".repositoryDetail.RepositoryDetailActivity"
      android:parentActivityName=".repositories.MainActivity" />
    <activity
            android:name=".commits.CommitsActivity"
            android:parentActivityName=".repositories.MainActivity" />
  </application>

</manifest>