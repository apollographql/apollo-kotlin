query FeedQuery($type: FeedType!, $limit: Int!) {
  feedEntries: feed(type: $type, limit: $limit) {
    ...EntryPreview
  }
}

fragment EntryPreview on Entry {
  id
  repository {
    ...RepositoryFragment
  }
  postedBy {
    login
  }
}

fragment RepositoryFragment on Repository {
  full_name
  owner {
    login
  }
}
