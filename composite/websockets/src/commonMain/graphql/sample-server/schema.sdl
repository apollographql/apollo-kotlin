
type Query {
  random: Int!
  count: Int!
}

type Subscription {
  countString(to: Int!, delayMillis: Int!): String!
  count(to: Int!, delayMillis: Int!): Int!
}

schema {
  query: Query
  subscription: Subscription
}
