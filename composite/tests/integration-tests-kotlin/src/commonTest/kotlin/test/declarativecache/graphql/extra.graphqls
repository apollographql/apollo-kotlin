extend type Author @client__typePolicy(keyFields: "firstName lastName")

extend type Book @client__typePolicy(keyFields: "isbn")

extend type Query @client__fieldPolicy(forField: "book", keyArgs: ["isbn"])

interface Node @client__typePolicy(keyFields: "id") {
  id: ID!
}

extend type Library implements Node

