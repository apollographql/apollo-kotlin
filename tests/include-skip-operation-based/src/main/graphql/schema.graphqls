type Query {
  animal: Animal
  animalById(id: ID!): Animal
  field: Int!
}

interface Animal {
  species: String
}

type Cat implements Animal {
  species: String
  meow: String
  mustaches: Int
}

type Dog implements Animal {
  species: String
  barf: String
}
