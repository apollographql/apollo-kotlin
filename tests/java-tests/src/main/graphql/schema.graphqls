type Query {
  random: Int!
  animal: Animal
}

interface Animal {
  species: String
  habitat: Habitat
}

type Habitat {
  kind: String
  temperature: Float
}

type Dog implements Animal {
  species: String
  habitat: Habitat
}

type Cat implements Animal {
  species: String
  habitat: Habitat
}