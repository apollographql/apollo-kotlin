
type Query {
  random: Int!
  time: Int!
}

type Subscription {
  count(delayMillis: Int!, to: Int!): Int!
  countString(delayMillis: Int!, to: Int!): String!
  time: Int!
  operationError: String!
}
