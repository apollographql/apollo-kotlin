query GetFoo {
  foo {
    bar @catch(to: THROW)
  }
}

query GetF {
  foo {
    f @catch(to: THROW)
  }
}

fragment FooFragment on Foo {
  f @catch(to: THROW)
}

query GetFFragmented {
  foo {
    ...FooFragment
  }
}
