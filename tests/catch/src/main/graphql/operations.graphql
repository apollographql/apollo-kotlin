query UserThrow {
  #
  user @catch(to: THROW){
    name
  }
}

query UserResult {
  user @catch(to: RESULT) {
    name
  }
}

query UserNull {
  user @catch(to: NULL) {
    name
  }
}

query UserIgnore {
  user @catch(if: false) {
    name
  }
}

