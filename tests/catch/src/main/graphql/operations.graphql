query User {
  user {
    name
  }
}

query UserResult {
  user @catch(to: RESULT) {
    name
  }
}#

query UserNull {
  user @catch(to: NULL) {
    name
  }
}

query Product {
  product {
    price
  }
}
query ProductIgnoreErrors @ignoreErrors {
  product {
    price
  }
}

