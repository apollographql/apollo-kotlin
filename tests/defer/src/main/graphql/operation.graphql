query Query1($a: Boolean!) {
  computers {
    id
    ...ComputerFields @defer(if: $a) @skip(if: $a)
  }
}
fragment ComputerFields on Computer {
  cpu
  year
  screen {
    resolution
    ...ScreenFields @defer
  }
}
fragment ScreenFields on Screen {
  isColor
}



query Query2 {
  computers {
    id
    ... on Computer @defer {
      cpu
      year
      screen {
        resolution
        ... on Screen @defer {
          isColor
        }
      }
    }
  }
}
