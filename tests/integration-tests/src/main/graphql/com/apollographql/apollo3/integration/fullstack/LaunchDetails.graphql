query LaunchDetails($id:ID!, $size: PatchSize! = LARGE) {
  launch(id: $id) {
    id
    site
    mission {
      name
      missionPatch(size: $size)
    }
    rocket {
      name
      type
    }
    isBooked
  }
}
