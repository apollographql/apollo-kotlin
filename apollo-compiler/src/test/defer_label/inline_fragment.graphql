query TestQuery {
  hero {
    ... on Droid @defer {
      name
    }
    ... on Droid @defer(label: "alreadySet") {
      name
    }
    ... on Droid @defer(if: true){
      name
    }
    ... on Droid @defer(label: "alreadySet", if: true) {
      name
    }
  }
}
