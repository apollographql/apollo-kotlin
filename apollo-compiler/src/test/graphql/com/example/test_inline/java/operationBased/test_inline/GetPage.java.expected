//
// AUTO-GENERATED FILE. DO NOT MODIFY.
//
// This class was automatically generated by Apollo GraphQL version '$VERSION'.
//
package com.example.test_inline;

import com.apollographql.apollo3.api.Adapter;
import com.apollographql.apollo3.api.CompiledField;
import com.apollographql.apollo3.api.CustomScalarAdapters;
import com.apollographql.apollo3.api.ObjectAdapter;
import com.apollographql.apollo3.api.Query;
import com.apollographql.apollo3.api.json.JsonWriter;
import com.example.test_inline.adapter.GetPage_ResponseAdapter;
import com.example.test_inline.selections.GetPageSelections;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.List;

public class GetPage implements Query<GetPage.Data> {
  public static final String OPERATION_ID = "09dd0a176a2233eccc3b2d3a76f25a1083460354f399f8b1aaf172c18cfc202b";

  /**
   * The minimized GraphQL document being sent to the server to save a few bytes.
   * The un-minimized version is:
   *
   * query GetPage {
   *   collection {
   *     __typename
   *     items {
   *       title
   *     }
   *     ... on ParticularCollection {
   *       items {
   *         __typename
   *         ... on ParticularItem {
   *           image
   *         }
   *       }
   *     }
   *   }
   * }
   */
  public static final String OPERATION_DOCUMENT = "query GetPage { collection { __typename items { title } ... on ParticularCollection { items { __typename ... on ParticularItem { image } } } } }";

  public static final String OPERATION_NAME = "GetPage";

  private transient volatile int $hashCode;

  private transient volatile boolean $hashCodeMemoized;

  private transient volatile String $toString;

  public GetPage() {

  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof GetPage) {
      return true;
    }
    return false;
  }

  @Override
  public int hashCode() {
    if (!$hashCodeMemoized) {
      int h = 1;
      $hashCode = h;
      $hashCodeMemoized = true;
    }
    return $hashCode;
  }

  @Override
  public String toString() {
    if ($toString == null) {
      $toString = "GetPage{"
        + "}";
    }
    return $toString;
  }

  @Override
  public String id() {
    return OPERATION_ID;
  }

  @Override
  public String document() {
    return OPERATION_DOCUMENT;
  }

  @Override
  public String name() {
    return OPERATION_NAME;
  }

  @Override
  public void serializeVariables(JsonWriter writer, CustomScalarAdapters customScalarAdapters)
      throws IOException {
    // This operation doesn't have any variable
  }

  @Override
  public Adapter<Data> adapter() {
    return new ObjectAdapter<Data>(GetPage_ResponseAdapter.Data.INSTANCE, false);
  }

  @Override
  public CompiledField rootField() {
    return new CompiledField.Builder(
      "data",
      com.example.test_inline.type.Query.type
    )
    .selections(GetPageSelections.root)
    .build();
  }

  public static class Data implements Query.Data {
    public Collection collection;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public Data(Collection collection) {
      this.collection = collection;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Data) {
        Data that = (Data) o;
        return ((this.collection == null) ? (that.collection == null) : this.collection.equals(that.collection));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (collection == null) ? 0 : collection.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Data{"
          + "collection=" + collection
          + "}";
      }
      return $toString;
    }
  }

  public static class Collection {
    public String __typename;

    public List<Item> items;

    /**
     * Synthetic field for inline fragment on ParticularCollection
     */
    public OnParticularCollection onParticularCollection;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public Collection(String __typename, List<Item> items,
        OnParticularCollection onParticularCollection) {
      this.__typename = __typename;
      this.items = items;
      this.onParticularCollection = onParticularCollection;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Collection) {
        Collection that = (Collection) o;
        return ((this.__typename == null) ? (that.__typename == null) : this.__typename.equals(that.__typename))
         &&((this.items == null) ? (that.items == null) : this.items.equals(that.items))
         &&((this.onParticularCollection == null) ? (that.onParticularCollection == null) : this.onParticularCollection.equals(that.onParticularCollection));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (__typename == null) ? 0 : __typename.hashCode();
        h *= 1000003;
        h ^= (items == null) ? 0 : items.hashCode();
        h *= 1000003;
        h ^= (onParticularCollection == null) ? 0 : onParticularCollection.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Collection{"
          + "__typename=" + __typename + ", "
          + "items=" + items + ", "
          + "onParticularCollection=" + onParticularCollection
          + "}";
      }
      return $toString;
    }
  }

  public static class Item {
    public String title;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public Item(String title) {
      this.title = title;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Item) {
        Item that = (Item) o;
        return ((this.title == null) ? (that.title == null) : this.title.equals(that.title));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (title == null) ? 0 : title.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Item{"
          + "title=" + title
          + "}";
      }
      return $toString;
    }
  }

  public static class OnParticularCollection {
    public List<Item1> items;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public OnParticularCollection(List<Item1> items) {
      this.items = items;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof OnParticularCollection) {
        OnParticularCollection that = (OnParticularCollection) o;
        return ((this.items == null) ? (that.items == null) : this.items.equals(that.items));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (items == null) ? 0 : items.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "OnParticularCollection{"
          + "items=" + items
          + "}";
      }
      return $toString;
    }
  }

  public static class Item1 {
    public String __typename;

    /**
     * Synthetic field for inline fragment on ParticularItem
     */
    public OnParticularItem onParticularItem;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public Item1(String __typename, OnParticularItem onParticularItem) {
      this.__typename = __typename;
      this.onParticularItem = onParticularItem;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Item1) {
        Item1 that = (Item1) o;
        return ((this.__typename == null) ? (that.__typename == null) : this.__typename.equals(that.__typename))
         &&((this.onParticularItem == null) ? (that.onParticularItem == null) : this.onParticularItem.equals(that.onParticularItem));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (__typename == null) ? 0 : __typename.hashCode();
        h *= 1000003;
        h ^= (onParticularItem == null) ? 0 : onParticularItem.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Item1{"
          + "__typename=" + __typename + ", "
          + "onParticularItem=" + onParticularItem
          + "}";
      }
      return $toString;
    }
  }

  public static class OnParticularItem {
    public String image;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public OnParticularItem(String image) {
      this.image = image;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof OnParticularItem) {
        OnParticularItem that = (OnParticularItem) o;
        return ((this.image == null) ? (that.image == null) : this.image.equals(that.image));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (image == null) ? 0 : image.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "OnParticularItem{"
          + "image=" + image
          + "}";
      }
      return $toString;
    }
  }
}
