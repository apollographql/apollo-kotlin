//
// AUTO-GENERATED FILE. DO NOT MODIFY.
//
// This class was automatically generated by Apollo GraphQL version '$VERSION'.
//
package com.example.fragments_with_defer_and_include_directives;

import com.apollographql.apollo3.api.Adapter;
import com.apollographql.apollo3.api.CompiledField;
import com.apollographql.apollo3.api.CustomScalarAdapters;
import com.apollographql.apollo3.api.ObjectAdapter;
import com.apollographql.apollo3.api.Query;
import com.apollographql.apollo3.api.json.JsonWriter;
import com.example.fragments_with_defer_and_include_directives.adapter.InlineMultipleWithDeferWithIf_ResponseAdapter;
import com.example.fragments_with_defer_and_include_directives.adapter.InlineMultipleWithDeferWithIf_VariablesAdapter;
import com.example.fragments_with_defer_and_include_directives.selections.InlineMultipleWithDeferWithIfSelections;
import java.io.IOException;
import java.lang.Boolean;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;

public class InlineMultipleWithDeferWithIf implements Query<InlineMultipleWithDeferWithIf.Data> {
  public static final String OPERATION_ID = "5b00be478e8052ac841db6890f937f8c56de7665b63ad6723da0572c0198f92d";

  /**
   * The minimized GraphQL document being sent to the server to save a few bytes.
   * The un-minimized version is:
   *
   * query InlineMultipleWithDeferWithIf($cond: Boolean!) {
   *   hero {
   *     __typename
   *     ... on Character @defer(label: "first", if: false) {
   *       id
   *     }
   *     ... on Character @defer(label: "second", if: true) {
   *       name
   *     }
   *     ... on Character @defer(label: "third", if: $cond) {
   *       name
   *     }
   *   }
   * }
   */
  public static final String OPERATION_DOCUMENT = "query InlineMultipleWithDeferWithIf($cond: Boolean!) { hero { __typename ... on Character @defer(label: \"first\", if: false) { id } ... on Character @defer(label: \"second\", if: true) { name } ... on Character @defer(label: \"third\", if: $cond) { name } } }";

  public static final String OPERATION_NAME = "InlineMultipleWithDeferWithIf";

  public final Boolean cond;

  private transient volatile int $hashCode;

  private transient volatile boolean $hashCodeMemoized;

  private transient volatile String $toString;

  public InlineMultipleWithDeferWithIf(Boolean cond) {
    this.cond = cond;
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof InlineMultipleWithDeferWithIf) {
      InlineMultipleWithDeferWithIf that = (InlineMultipleWithDeferWithIf) o;
      return ((this.cond == null) ? (that.cond == null) : this.cond.equals(that.cond));
    }
    return false;
  }

  @Override
  public int hashCode() {
    if (!$hashCodeMemoized) {
      int h = 1;
      h *= 1000003;
      h ^= (cond == null) ? 0 : cond.hashCode();
      $hashCode = h;
      $hashCodeMemoized = true;
    }
    return $hashCode;
  }

  @Override
  public String toString() {
    if ($toString == null) {
      $toString = "InlineMultipleWithDeferWithIf{"
        + "cond=" + cond
        + "}";
    }
    return $toString;
  }

  @Override
  public String id() {
    return OPERATION_ID;
  }

  @Override
  public String document() {
    return OPERATION_DOCUMENT;
  }

  @Override
  public String name() {
    return OPERATION_NAME;
  }

  @Override
  public void serializeVariables(JsonWriter writer, CustomScalarAdapters customScalarAdapters)
      throws IOException {
    InlineMultipleWithDeferWithIf_VariablesAdapter.INSTANCE.toJson(writer, customScalarAdapters, this);
  }

  @Override
  public Adapter<Data> adapter() {
    return new ObjectAdapter<Data>(InlineMultipleWithDeferWithIf_ResponseAdapter.Data.INSTANCE, false);
  }

  @Override
  public CompiledField rootField() {
    return new CompiledField.Builder(
      "data",
      com.example.fragments_with_defer_and_include_directives.type.Query.type
    )
    .selections(InlineMultipleWithDeferWithIfSelections.root)
    .build();
  }

  public static class Data implements Query.Data {
    public Hero hero;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public Data(Hero hero) {
      this.hero = hero;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Data) {
        Data that = (Data) o;
        return ((this.hero == null) ? (that.hero == null) : this.hero.equals(that.hero));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (hero == null) ? 0 : hero.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Data{"
          + "hero=" + hero
          + "}";
      }
      return $toString;
    }
  }

  public static class Hero {
    public String __typename;

    /**
     * Synthetic field for inline fragment on Character
     */
    public OnCharacter onCharacter;

    /**
     * Synthetic field for inline fragment on Character
     */
    public OnCharacterDeferSecond onCharacterDeferSecond;

    /**
     * Synthetic field for inline fragment on Character
     */
    public OnCharacterDeferThird onCharacterDeferThird;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public Hero(String __typename, OnCharacter onCharacter,
        OnCharacterDeferSecond onCharacterDeferSecond,
        OnCharacterDeferThird onCharacterDeferThird) {
      this.__typename = __typename;
      this.onCharacter = onCharacter;
      this.onCharacterDeferSecond = onCharacterDeferSecond;
      this.onCharacterDeferThird = onCharacterDeferThird;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Hero) {
        Hero that = (Hero) o;
        return ((this.__typename == null) ? (that.__typename == null) : this.__typename.equals(that.__typename))
         &&((this.onCharacter == null) ? (that.onCharacter == null) : this.onCharacter.equals(that.onCharacter))
         &&((this.onCharacterDeferSecond == null) ? (that.onCharacterDeferSecond == null) : this.onCharacterDeferSecond.equals(that.onCharacterDeferSecond))
         &&((this.onCharacterDeferThird == null) ? (that.onCharacterDeferThird == null) : this.onCharacterDeferThird.equals(that.onCharacterDeferThird));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (__typename == null) ? 0 : __typename.hashCode();
        h *= 1000003;
        h ^= (onCharacter == null) ? 0 : onCharacter.hashCode();
        h *= 1000003;
        h ^= (onCharacterDeferSecond == null) ? 0 : onCharacterDeferSecond.hashCode();
        h *= 1000003;
        h ^= (onCharacterDeferThird == null) ? 0 : onCharacterDeferThird.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Hero{"
          + "__typename=" + __typename + ", "
          + "onCharacter=" + onCharacter + ", "
          + "onCharacterDeferSecond=" + onCharacterDeferSecond + ", "
          + "onCharacterDeferThird=" + onCharacterDeferThird
          + "}";
      }
      return $toString;
    }
  }

  public static class OnCharacter {
    /**
     * The ID of the character
     */
    public String id;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public OnCharacter(String id) {
      this.id = id;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof OnCharacter) {
        OnCharacter that = (OnCharacter) o;
        return ((this.id == null) ? (that.id == null) : this.id.equals(that.id));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (id == null) ? 0 : id.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "OnCharacter{"
          + "id=" + id
          + "}";
      }
      return $toString;
    }
  }

  public static class OnCharacterDeferSecond {
    /**
     * The name of the character
     */
    public String name;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public OnCharacterDeferSecond(String name) {
      this.name = name;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof OnCharacterDeferSecond) {
        OnCharacterDeferSecond that = (OnCharacterDeferSecond) o;
        return ((this.name == null) ? (that.name == null) : this.name.equals(that.name));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (name == null) ? 0 : name.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "OnCharacterDeferSecond{"
          + "name=" + name
          + "}";
      }
      return $toString;
    }
  }

  public static class OnCharacterDeferThird {
    /**
     * The name of the character
     */
    public String name;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public OnCharacterDeferThird(String name) {
      this.name = name;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof OnCharacterDeferThird) {
        OnCharacterDeferThird that = (OnCharacterDeferThird) o;
        return ((this.name == null) ? (that.name == null) : this.name.equals(that.name));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int h = 1;
        h *= 1000003;
        h ^= (name == null) ? 0 : name.hashCode();
        $hashCode = h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "OnCharacterDeferThird{"
          + "name=" + name
          + "}";
      }
      return $toString;
    }
  }
}
