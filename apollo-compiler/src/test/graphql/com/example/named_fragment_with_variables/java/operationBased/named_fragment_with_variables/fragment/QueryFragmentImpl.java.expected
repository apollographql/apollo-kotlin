//
// AUTO-GENERATED FILE. DO NOT MODIFY.
//
// This class was automatically generated by Apollo GraphQL version '$VERSION'.
//
package com.example.named_fragment_with_variables.fragment;

import com.apollographql.apollo3.api.Adapter;
import com.apollographql.apollo3.api.CompiledField;
import com.apollographql.apollo3.api.CustomScalarAdapters;
import com.apollographql.apollo3.api.Fragment;
import com.apollographql.apollo3.api.ObjectAdapter;
import com.apollographql.apollo3.api.json.JsonWriter;
import com.example.named_fragment_with_variables.fragment.adapter.QueryFragmentImpl_VariablesAdapter;
import com.example.named_fragment_with_variables.fragment.selections.QueryFragmentSelections;
import com.example.named_fragment_with_variables.type.Query;
import com.example.named_fragment_with_variables.type.UserQuery;
import java.io.IOException;
import java.lang.Integer;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;

public class QueryFragmentImpl implements Fragment<QueryFragment> {
  public final String organizationId;

  public final UserQuery query;

  public final Integer size;

  private transient volatile int $hashCode;

  private transient volatile boolean $hashCodeMemoized;

  private transient volatile String $toString;

  public QueryFragmentImpl(String organizationId, UserQuery query, Integer size) {
    this.organizationId = organizationId;
    this.query = query;
    this.size = size;
  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof QueryFragmentImpl) {
      QueryFragmentImpl that = (QueryFragmentImpl) o;
      return ((this.organizationId == null) ? (that.organizationId == null) : this.organizationId.equals(that.organizationId))
       &&((this.query == null) ? (that.query == null) : this.query.equals(that.query))
       &&((this.size == null) ? (that.size == null) : this.size.equals(that.size));
    }
    return false;
  }

  @Override
  public int hashCode() {
    if (!$hashCodeMemoized) {
      int h = 1;
      h *= 1000003;
      h ^= (organizationId == null) ? 0 : organizationId.hashCode();
      h *= 1000003;
      h ^= (query == null) ? 0 : query.hashCode();
      h *= 1000003;
      h ^= (size == null) ? 0 : size.hashCode();
      $hashCode = h;
      $hashCodeMemoized = true;
    }
    return $hashCode;
  }

  @Override
  public String toString() {
    if ($toString == null) {
      $toString = "QueryFragmentImpl{"
        + "organizationId=" + organizationId + ", "
        + "query=" + query + ", "
        + "size=" + size
        + "}";
    }
    return $toString;
  }

  @Override
  public void serializeVariables(JsonWriter writer, CustomScalarAdapters customScalarAdapters)
      throws IOException {
    QueryFragmentImpl_VariablesAdapter.INSTANCE.toJson(writer, customScalarAdapters, this);
  }

  @Override
  public Adapter<QueryFragment> adapter() {
    return new ObjectAdapter<QueryFragment>(QueryFragmentImpl_ResponseAdapter.QueryFragment.INSTANCE, false);
  }

  @Override
  public CompiledField rootField() {
    return new CompiledField.Builder(
      "data",
      Query.type
    )
    .selections(QueryFragmentSelections.root)
    .build();
  }
}
