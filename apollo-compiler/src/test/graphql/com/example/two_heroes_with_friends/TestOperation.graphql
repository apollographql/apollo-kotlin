query TestQuery {
  r2: hero {
    name
    friendsConnection {
      totalCount
      edges {
        node {
          name
        }
      }
    }
  }
  luke: hero(episode: EMPIRE) {
    id
    name
    friendsConnection {
      totalCount
      edges {
        node {
          name
        }
      }
    }
  }
}
