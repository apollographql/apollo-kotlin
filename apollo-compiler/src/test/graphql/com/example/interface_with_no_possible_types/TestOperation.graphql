# The A interface has no possibleTypes so we can potentially skip the generation
query TestQuery {
    root {
        ... on A {
            fieldA1
            ... on B {
                fieldB1
            }
        }
    }
}
