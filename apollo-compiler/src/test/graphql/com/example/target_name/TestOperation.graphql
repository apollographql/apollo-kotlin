query TestQuery($input: ReservedInput, $scalar: ReservedScalar, $enum: ReservedEnum, $enum2: renamedEnum, $enum3: RenamedEnum) {
  object(input: $input) {
    field
  }
  interface {
    field
  }
  union {
    ... on ReservedObject {
      field
    }
  }
  scalar(scalar: $scalar)
  enum(enum: $enum)
  enum2(enum2: $enum2)
  enum3(enum3: $enum3)
}
