package com.apollostack.android.gradle

import org.gradle.api.tasks.SourceTask

public class ApolloClassgenTask extends SourceTask {
    // TODO: implement once we have error handling and validation for the IR file, implementation for now would be as simple as a call to the compiler
}
