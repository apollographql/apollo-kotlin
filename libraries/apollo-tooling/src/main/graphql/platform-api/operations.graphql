query DownloadSchema($graphID: ID!, $variant: String!) {
  graph(id: $graphID) {
    variant(name: $variant) {
      latestPublication {
        schema {
          document
        }
      }
    }
  }
}
