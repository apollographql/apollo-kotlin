type Query {
    field(arg: SomeInputObject): String
}

# Input object `A` references itself through an unbreakable chain of input fields and cannot be constructed: A.b --> B.c --> C.a --> A
input A {
    b: B!
}

input B {
    c: C!
}

input C {
    a: A!
}