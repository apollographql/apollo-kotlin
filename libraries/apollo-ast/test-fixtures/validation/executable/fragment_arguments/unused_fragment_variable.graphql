# PRAGMA allowFragmentArguments

fragment UserFields($includeEmail: Boolean!, $unused: String) on User {
    name
    email @include(if: $includeEmail)
}

query GetUser {
    user(id: "1") {
        ...UserFields(includeEmail: true)
    }
}
