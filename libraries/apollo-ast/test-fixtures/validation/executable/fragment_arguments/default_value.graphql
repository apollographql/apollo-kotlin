# PRAGMA allowFragmentArguments

fragment UserFields($includeEmail: Boolean! = true) on User {
    name
    email @include(if: $includeEmail)
}

query GetUser {
    user(id: "1") {
        ...UserFields
    }
}
