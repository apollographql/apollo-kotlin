# PRAGMA allowFragmentArguments

fragment UserFields($includeEmail: Boolean!) on User {
    name
    email @include(if: $includeEmail)
}

query GetUser {
    user(id: "1") {
        ...UserFields(includeEmail: true, unknownArg: "foo")
    }
}
