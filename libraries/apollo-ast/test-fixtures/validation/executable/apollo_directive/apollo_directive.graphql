
query TestQuery {
    # The schema directive takes precedence over the linked one. Since the schema one has a required argument, this does not validate
    field @nonnull
    # This validates
    alias: field @nonnull(required: "foo")
}
