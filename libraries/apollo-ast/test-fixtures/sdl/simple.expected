schema {
    query: Query
}

type Query {
    foo: Int
}

interface A {
    a: String
}

interface B {
    b: String
}

type C implements A & B {
    a: String

    b: String
}

interface D implements A & B {
    a: String

    b: String
}

directive @myDirective1(arg1: String) on DIRECTIVE_DEFINITION

directive @myDirective2(arg1: String, arg2: Int = 42) @deprecated(reason: "Use newDirective instead") @myDirective1(arg1: "test") on FIELD | FRAGMENT_SPREAD | INLINE_FRAGMENT
