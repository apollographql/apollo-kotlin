type Query {
  enum: Enum
}
extend schema @link(url: "https://specs.apollo.dev/kotlin_labs/v0.7/", import: ["@targetName"])

enum Enum {
  type
}

enum Enum4 {
  foo
  bar @targetName(name: "foo")
}

enum Enum5 {
  foo @targetName(name: "bar")
  bar2 @targetName(name: "bar")
}
