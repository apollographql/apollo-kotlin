//
// AUTO-GENERATED FILE. DO NOT MODIFY.
//
// This class was automatically generated by Apollo GraphQL version '$VERSION'.
//
package com.example.max_age;

import com.apollographql.apollo.api.Adapter;
import com.apollographql.apollo.api.CompiledField;
import com.apollographql.apollo.api.CustomScalarAdapters;
import com.apollographql.apollo.api.ObjectAdapter;
import com.apollographql.apollo.api.Query;
import com.apollographql.apollo.api.json.JsonWriter;
import com.example.max_age.adapter.GetCachedBookCachedTitle_ResponseAdapter;
import com.example.max_age.selections.GetCachedBookCachedTitleSelections;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;

public class GetCachedBookCachedTitle implements Query<GetCachedBookCachedTitle.Data> {
  public static final String OPERATION_ID = "0bb38df8c0f604bee24db746b835fa2f72377e9df3bc5c2cbbafa5c4012abb11";

  /**
   * The minimized GraphQL document being sent to the server to save a few bytes.
   * The un-minimized version is:
   *
   * query GetCachedBookCachedTitle {
   *   cachedBook {
   *     cachedTitle
   *   }
   * }
   */
  public static final String OPERATION_DOCUMENT = "query GetCachedBookCachedTitle { cachedBook { cachedTitle } }";

  public static final String OPERATION_NAME = "GetCachedBookCachedTitle";

  private transient volatile int $hashCode;

  private transient volatile boolean $hashCodeMemoized;

  private transient volatile String $toString;

  public GetCachedBookCachedTitle() {

  }

  @Override
  public boolean equals(Object o) {
    if (o == this) {
      return true;
    }
    if (o instanceof GetCachedBookCachedTitle) {
      return true;
    }
    return false;
  }

  @Override
  public int hashCode() {
    if (!$hashCodeMemoized) {
      int __h = 1;
      $hashCode = __h;
      $hashCodeMemoized = true;
    }
    return $hashCode;
  }

  @Override
  public String toString() {
    if ($toString == null) {
      $toString = "GetCachedBookCachedTitle{"
        + "}";
    }
    return $toString;
  }

  public static Builder builder() {
    return new Builder();
  }

  @Override
  public String id() {
    return OPERATION_ID;
  }

  @Override
  public String document() {
    return OPERATION_DOCUMENT;
  }

  @Override
  public String name() {
    return OPERATION_NAME;
  }

  @Override
  public void serializeVariables(JsonWriter writer, CustomScalarAdapters customScalarAdapters,
      boolean withDefaultValues) throws IOException {
    // This operation doesn't have any variable
  }

  @Override
  public Adapter<Data> adapter() {
    return new ObjectAdapter<Data>(GetCachedBookCachedTitle_ResponseAdapter.Data.INSTANCE, false);
  }

  @Override
  public CompiledField rootField() {
    return new CompiledField.Builder(
      "data",
      com.example.max_age.type.Query.type
    )
    .selections(GetCachedBookCachedTitleSelections.__root)
    .build();
  }

  public static final class Builder {
    Builder() {
    }

    public GetCachedBookCachedTitle build() {
      return new GetCachedBookCachedTitle();
    }
  }

  public static class Data implements Query.Data {
    public CachedBook cachedBook;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public Data(CachedBook cachedBook) {
      this.cachedBook = cachedBook;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof Data) {
        Data that = (Data) o;
        return ((this.cachedBook == null) ? (that.cachedBook == null) : this.cachedBook.equals(that.cachedBook));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int __h = 1;
        __h *= 1000003;
        __h ^= (cachedBook == null) ? 0 : cachedBook.hashCode();
        $hashCode = __h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "Data{"
          + "cachedBook=" + cachedBook
          + "}";
      }
      return $toString;
    }
  }

  public static class CachedBook {
    public String cachedTitle;

    private transient volatile int $hashCode;

    private transient volatile boolean $hashCodeMemoized;

    private transient volatile String $toString;

    public CachedBook(String cachedTitle) {
      this.cachedTitle = cachedTitle;
    }

    @Override
    public boolean equals(Object o) {
      if (o == this) {
        return true;
      }
      if (o instanceof CachedBook) {
        CachedBook that = (CachedBook) o;
        return ((this.cachedTitle == null) ? (that.cachedTitle == null) : this.cachedTitle.equals(that.cachedTitle));
      }
      return false;
    }

    @Override
    public int hashCode() {
      if (!$hashCodeMemoized) {
        int __h = 1;
        __h *= 1000003;
        __h ^= (cachedTitle == null) ? 0 : cachedTitle.hashCode();
        $hashCode = __h;
        $hashCodeMemoized = true;
      }
      return $hashCode;
    }

    @Override
    public String toString() {
      if ($toString == null) {
        $toString = "CachedBook{"
          + "cachedTitle=" + cachedTitle
          + "}";
      }
      return $toString;
    }
  }
}
